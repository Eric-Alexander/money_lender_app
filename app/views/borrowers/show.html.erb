<h1> Hey there, <%= @borrower.first_name %> <%= @borrower.last_name %></h1>
<h2> Amount Needed: <%= @borrower.money_needed %> </h2>
<h2> Amount Raised: <%= @borrower.money_raised %> </h2>

<div class="col col-md-8">
  <h3>List of people who lent you money:</h3>
  <table class="table table-striped- table-hover">
    <thead>
      <tr>
        <th> Name: </th>
        <th> Email: </th>
        <th> Amount Lent: </th>
      </tr>
    </thead>
    <tbody>
      <% @loans.each do |x| %>
      <tr>
        <td><%= x.first_name %> <%= x.last_name %> </td>
        <td><%= x.email %></td>
        <td><%= x.amount %> </td>
      </tr>
      <% end %>
    </tbody>
  </table>

<%= link_to "Log out", session_path, method: "delete" %>
